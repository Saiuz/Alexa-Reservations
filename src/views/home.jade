
include nav_common.jade

.container
  .row
    .col-md-4
      .panel.panel-default
        .panel-heading Ankunft {{theDate | date:'dd.MM.yyyy'}}
        .panel-body.scrollable200
          ax-reservation-list(list-mode="arrival" list-date="theDate" selected-reservation="selectedReservation")
      .panel.panel-default
        .panel-heading Abfahrt {{theDate | date:'dd.MM.yyyy'}}
        .panel-body.scrollable200
          ax-reservation-list(list-mode="departure" list-date="theDate" selected-reservation="selectedReservation")
      .panel.panel-default
        .panel-heading Abfahrt {{theNextDate | date:'dd.MM.yyyy'}}
        .panel-body.scrollable200
          ax-reservation-list(list-mode="departure" list-date="theNextDate" selected-reservation="selectedReservation")
    .col-md-6
      .panel.panel-default(ng-hide="selectedReservation === 0")
        .panel-heading
          .form-horizontal
            span.input-group-btn Reservierung #: {{selectedReservation}}
              button.btn.btn-small.pull-right(type="button" ng-click="clearSelected()")
                i.glyphicon.glyphicon-eye-close
        .panel-body
          div(ax-reservation-details reservation="selectedReservation")
    .col-md-5
      .panel.panel-default
        .panel-heading Wochen Plan für {{weekStart | date:'dd.MM.yyyy'}} bis {{weekEnd | date:'dd.MM.yyyy'}}
          div
            a.btn.btn-default.btn-small(href="#" ng-click="goPrevWeek()")
              span.glyphicon.glyphicon-fast-backward
            a.btn.btn-default.btn-small(href="#" ng-click="goPrev()")
              span.glyphicon.glyphicon-backward
            a.btn.btn-default.btn-amall(href="#" ng-click="goToday()")
              span Heute
            a.btn.btn-default.btn-small(href="#" ng-click="goNext()")
              span.glyphicon.glyphicon-forward
            a.btn.btn-default.btn-small(href="#" ng-click="goNextWeek()")
              span.glyphicon.glyphicon-fast-forward
        .panel-body
          div(ax-reservation-stats date-in-week="theDate")
